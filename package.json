{
  "name": "teste-table-filter",
  "version": "1.0.0",
  "description": "A simple test with searchable table",
  "main": "app/index.html",
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vitor Makino",
  "license": "ISC",
  "dependencies": {
    "bower": "^1.4.1",
    "http-server": "^0.8.0"
  }
}
