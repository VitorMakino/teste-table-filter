<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Filter</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
	  <form novalidate class="navbar-form navbar-left" role="search" name="searchForm">
	    <div class="form-group" ng-repeat="filter in filters">
		  <select ng-model="filter.name" class="form-control" required name="name">
		    <option value="">-- choose column --</option>
			<option ng-repeat="column in columns" value="{{column.property}}">{{column.name}}</option>
		  </select>
		   <select ng-model="filter.operator" ng-options="operator for operator in operators" class="form-control" required name="operator" >
			<option value="">-- choose operator --</option>
		  </select>
		  <input type="text" class="form-control" id="{{filter.name$index}}" placeholder="" ng-model="filter.value" required name="value" ng-model-options="{ updateOn: 'blur' }">
		  <button type="remove" class="btn btn-default" ng-click="removeFilter(filter)" ng-show="showRemoveButton()">Remove Filter</button>
	  </div>
	  <button type="button" class="btn btn-default  navbar-btn" ng-click="addFilter()" ng-disabled="!isValid()">Add Filter</button>
	 </form>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
